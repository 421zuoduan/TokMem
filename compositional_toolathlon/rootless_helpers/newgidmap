#!/usr/bin/env bash
set -euo pipefail

HELPER_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHON_BIN="${TOOLATHLON_ROOTLESS_PYTHON:-python3}"
exec "$PYTHON_BIN" "$HELPER_DIR/idmap_helper.py" gid "$@"
