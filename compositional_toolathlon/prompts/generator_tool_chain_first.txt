Create one original multi-step local task from the supplied MCP schemas.
If must_require_tool_ids is present, place every listed tool in the necessary
dependency graph; do not merely list it as available.
Always include required_terminal_tool_id in available_tools.
The requested work must create or modify at least one workspace file. The
evaluator must fail on initial_workspace and pass after oracle_final_state.
Do not require unrelated files to remain unchanged unless the evaluator uses
tree_equals to enforce that condition.
Do not state broad preservation of workbook contents or formatting. Mention
only cells, sheets, merged ranges, tables, charts, and styles that the
evaluator explicitly checks.
If the evaluator requires exact text, CSV, or JSON, the instruction or source
files must specify every label, delimiter, ordering rule, and newline needed
to derive it.

First define a 2-15 step dependency graph. At least one later argument must be
derived from an earlier real observation. Then create deterministic fictional
assets, oracle state, a workspace_assertions_v1 evaluator, and a concise user
instruction. Follow the supplied declarative task-spec contract and relative
paths; never emit evaluator code. Include at least three distinct real but
irrelevant tools. Do not use external services or any benchmark prompt,
entity, file, answer, or trajectory.

Return task-spec JSON only. Do not include prose, hidden reasoning, or a
solution trajectory.
