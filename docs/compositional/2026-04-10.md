# TokMem 梳理

Atomic 实验里, 一个 memory token 对应一个 procedure/task; Compositional 实验里, 一个 memory token 对应一个 tool, 输出 tool + 参数 (arguments), 回答仅包括 tool 和参数, 所以论文里表三给的是 F1 Score




## Atomic 实验

只需预测一个 memory token, 一个 memory token 路由到一个 procedure/task

- 训练数据形式: `<m> + response`, 仅使用 CE loss 训练
- 数据量: 700 task, 每个 task 有 500 train + 10 val + 50 test






# Compositional exps

## 数据集

### APIGen - 原始数据集

从 APIGen 数据集里抽取 atomic 样本, 组合成 `<m1> + response1 + <m2> + response2` 的形式

- query 字段
- answers 字段:  
    - 一个 tool, 单次/多次调用不管
    - 多个 tool
    - 统计某个 tool 出现频率时仅考虑一个 tool 的情况, answers 字段可看作真实答案
- tools 字段: 完成当前 query 可用的 tools 列表与参数
- APIGen (原始数据集) 评估时选对 tool 且参数传对就认为是模型回答正确

```json
  {
    "id": 53263,
    "query": "Update the endpoint with info1 as 'update1', info2 as 'update2', and info3 as 'update3'. Also, find the blank label sheet brands
  for A4 format.",
    "answers": [
      {
        "name": "update",
        "arguments": {
          "info3": "update3",
          "info1": "update1",
          "info2": "update2"
        }
      },
      {
        "name": "label_template_brands",
        "arguments": {
          "format": "A4"
        }
      }
    ],
    "tools": [
      {
        "name": "label_template_brands",
        "description": "Returns the list of blank label sheet brands supported by Ecardify on the given date.",
        "parameters": {
          "format": {
            "description": "The paper size format, which can be either \"Letter\" (8.5 inch by 11 inch) or \"A4\" (210 mm by 297 mm).",
            "type": "str",
            "default": "a4"
          }
        }
      },
      {
        "name": "search",
        "description": "This function performs a search query using the Market Intelligence by Automata API, returning information about
  related companies based on the provided search terms.",
        "parameters": {
          "terms": {
            "description": "Search terms separated by commas, which are used to filter the companies. Example: 'cloud
  computing,enterprise,security'.",
            "type": "str",
            "default": ""
          },
          "page": {
            "description": "Page number for paginated search results. Default is '0'. Example: '0', '1', '2', etc.",
            "type": "str, optional",
            "default": "0"
          }
        }
      },
      {
        "name": "update",
        "description": "Updates data on a specified endpoint using the provided information and RapidAPI key.",
        "parameters": {
          "info3": {
            "description": "The third piece of information to be sent as a query parameter.",
            "type": "str",
            "default": "333"
          },
          "info1": {
            "description": "The first piece of information to be sent as a query parameter.",
            "type": "str",
            "default": "111"
          },
          "info2": {
            "description": "The second piece of information to be sent as a query parameter.",
            "type": "str",
            "default": "222"
          }
        }
      },
      {
        "name": "label_templates",
        "description": "Fetches a list of label templates filtered by various parameters such as paper format, brand, label size, and shape.",
        "parameters": {
          "format": {
            "description": "The paper format, either 'Letter' (8.5 inch by 11 inch) or 'A4' (210 mm by 297 mm).",
            "type": "str",
            "default": "letter"
          },
          "brand": {
            "description": "The name of the label manufacturer. Default is 'avery'. Possible values include 'Avery', 'Herma', 'Onlinelabels',
  'Uline', 'Megastar', 'Sheetlabels'.",
            "type": "str, optional",
            "default": "avery"
          },
          "height": {
            "description": "The height of the label.",
            "type": "str, optional",
            "default": ""
          },
          "code": {
            "description": "A string representing approximate label sizes in the format [width]x[height]. Default is '2x2'.",
            "type": "str, optional",
            "default": "2x2"
          },
          "width": {
            "description": "The width of the label.",
            "type": "str, optional",
            "default": ""
          },
          "shape": {
            "description": "The shape of the label. Valid values include 'square', 'rectangle', 'circle', 'oval'.",
            "type": "str, optional",
            "default": ""
          }
        }
      }
    ]
  }
```


### 组合数据集

选取高频 tool, 利用这些 tool 合成 Compositional 数据

- 训练数据:
    - 真实数据: 单/多次调用样本直接当作训练数据, 共 50tool × 50 条数据 
    - 合成数据: 
        1. 随机选 2/3 个 tool, 每个 tool 取一个 query-call pair (这里叫 call 是因为可能是单次调用也可能是多次调用)
        2. 使用连接词拼接多个 query, 按顺序拼接 tools 和 calls. 要求 function call 次数最大为 4
        3. 2tool 和 3tool 各 1250 条
- 测试数据合成同上, 2tool 和 3tool 各 250 条, 无单 tool 数据


问题:

1. 模型选对 tool, 回答对 argument 就算回答正确, 没有做更复杂的正确性判断
2. 


## 训练过程

纯 CE loss, 进入下一个 memory token 前没有一个标志上一个 memory token 控制生成结束的 token



## 思路

另外有一个问题: 这种 Compositional 的输出格式是不是在 adaptation 中被学到了? 可不可以再加一个 loss 用以约束让输出格式与新的输出格式更接近? 或者有几个固定的 token 专门让模型适应这种输出? 前几个 token 在训练的时候让模型固定输出, 引导出在 compositional tool use 这种 setting 上的能力; 这样也能把 atomic 的实验放进来, 如果输出是某个 atomic 的 token 就完成它的任务, 如果输出是 tool 这边的 token, 那就执行 tool 这边的流程. 这样需要在输入的最前面手动加几个自己做的 token




routing 的问题: 没有一个标志上一个 memory token 控制范围内生成结束的 token, 可以加一个控制生成结束的 token, 将这个路由显式化控制, 这样也能加一个 routing loss

任务形式的问题: 如果换成真正调用工具去完成任务的任务, 就不用改输出形式, 也就不需要 adaptation

